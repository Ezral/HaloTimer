# HaloTimer 1.2.2

- Fix diagonal lines during wrapped animations in every fullscreen section and the overlay edge lanes.
- Extract the end and beginning of a wrapped stroke into independent paths, then combine them explicitly. This works with graphics engines that append extracted segments and engines that replace them; neither segment can originate at the canvas origin.
- Preserve smooth wraparound, rounded fullscreen stroke ends, pinch-controlled headers, timer geometry, and existing settings.
- Expand regression coverage across all one-, two- and three-timer Pizza/Split layouts, both orientations, timer positions, corner/inset/line settings, complete animation cycles, and all overlay alert styles. Reproduce destination replacement independently of the emulator's graphics version.
- Run native regression tests on Android 15 and Android 16.

Version 1.2.2 (12). The existing package and signing identity are retained for updates over 1.2.1.
